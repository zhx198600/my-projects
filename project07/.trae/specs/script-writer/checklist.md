# 创意剧本生成器 - 验证检查清单

## 项目基础架构
- [x] 项目可通过 `npm run dev` 成功启动，无控制台错误
- [x] Tailwind CSS 样式正确应用，UI 组件样式符合设计
- [x] TypeScript 编译无类型错误，严格模式启用
- [x] 目录结构清晰：components, hooks, types, mock, utils 等目录存在

## 类型系统
- [x] Character 类型定义完整，包含姓名、描述、对话样式等字段
- [x] Scene 类型定义完整，包含场景描述、位置、时间等字段
- [x] Episode 类型定义完整，包含集数、标题、场景列表、角色列表、对话列表等
- [x] Script 类型定义完整，正确嵌套包含 Episode 数组

## Mock数据与生成服务
- [x] generateScript 函数返回符合 Script 类型的数据
- [x] 生成过程有 2-5 秒的延迟模拟，不可瞬时完成
- [x] 生成的剧本包含 3-5 集内容，非更多也非更少
- [x] Mock 数据中至少包含 10 套不同的剧本模板
- [x] 结局反转设计合理，有伏笔支撑且逻辑自洽

## 输入页面功能
- [x] 输入少于 10 个字符时，"生成剧本"按钮禁用
- [x] 输入超过 500 个字符时，阻止继续输入并显示警告
- [x] 字数统计实时更新，显示当前字数 / 限制字数
- [x] 输入框支持多行输入，换行正常工作

## 加载状态
- [x] 点击生成按钮后显示加载动画/遮罩
- [x] 加载期间用户无法操作输入框或再次点击生成
- [x] 加载过程显示进度提示文案（如"正在构思..."、"构建角色..."）
- [x] 进度文案在加载期间会动态变化，非静止不变

## 剧本展示功能
- [x] 点击剧集导航标签正确切换显示对应集数内容
- [x] 当前选中的集数有明显的视觉高亮样式
- [x] 场景描述、角色名称、对话内容有明显的样式区分
- [x] 对话使用气泡或缩进区分不同角色，阅读体验良好
- [x] 内容过长时可正常滚动，无溢出或截断

## 存储与导出功能
- [x] 保存剧本后，数据写入 localStorage 可通过开发工具验证
- [x] 刷新页面后，可从 localStorage 恢复最后一次保存的剧本
- [x] 导出 TXT 文件功能正常，文件可下载
- [x] 导出的 TXT 文件格式正确，包含集数分隔、标题、场景描述和对话
- [x] 复制到剪贴板功能正常，粘贴后内容完整

## 状态与路由
- [x] 从输入页 → 加载页 → 结果页的状态流转正确
- [x] 从结果页可返回到输入页重新开始
- [x] Context 状态跨组件正确传递，类型推断正常

## 响应式布局
- [x] 桌面端（≥1024px）布局合理，两侧留白适中
- [x] 移动端（375px宽度）无内容溢出或水平滚动条
- [x] 平板端（768px宽度）布局自然过渡，无挤压感
- [x] 触摸可点击区域最小 44x44px，便于手指操作

## 交互与体验
- [x] 按钮有 hover/active 状态，点击反馈明显
- [x] 输入框 focus 状态有样式提示
- [x] 页面过渡动画流畅，不卡顿
- [x] 结局揭示时有轻微动画效果增强惊喜感

## 内容质量（主观验证）
- [ ] 剧本整体结构完整，有开端、发展、高潮、结局
- [ ] 角色对话符合人物设定，不生硬
- [ ] 结局确实出人意料，但回顾前文有伏笔可循
- [ ] 用户体验流畅，从输入到获得结果的流程清晰直观

## 完整流程端到端验证
- [ ] 完整流程测试：输入创意 → 等待生成 → 查看各集 → 保存 → 导出 → 返回
- [ ] 多次测试：同一输入/不同输入多次生成，验证结果多样性
- [ ] 边界测试：输入刚好10字、输入刚好500字、输入特殊字符
